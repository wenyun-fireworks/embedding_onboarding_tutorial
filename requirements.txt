# Client-side tooling for the tutorial. Training runs on Fireworks infra (the
# Training SDK provisions the GPU trainer for you), so you do NOT need a local
# GPU or torch build for training.
#
# The Fireworks Training SDK + cookbook recipes provide the trainer API used in
# step 2 (`training.recipes.embedding_loop`). Install them from the public repos:
#   pip install fireworks-ai            # embeddings/inference client + firectl auth
#   pip install tinker                  # trainer protocol used by the cookbook recipe
#   git clone https://github.com/fw-ai/cookbook && pip install -e cookbook
#
# Everything below is only needed on the client for data prep + evaluation:
numpy>=1.26
requests>=2.31
pytrec_eval-terrier>=0.5.6
python-dotenv>=1.0
# Client-side tokenizer for the step-6 input-form invariance check (raw text vs
# pre-tokenized input_ids). Needs the model's tokenizer, e.g. Qwen/Qwen3-0.6B.
transformers>=4.51
